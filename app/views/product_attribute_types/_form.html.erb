<%= form_for(@product_attribute_type) do |f| %>
  <% if @product_attribute_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_attribute_type.errors.count, "error") %> prohibited this product_attribute_type from being saved:</h2>

      <ul>
      <% @product_attribute_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
