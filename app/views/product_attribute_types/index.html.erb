<h1>Listing product_attribute_types</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @product_attribute_types.each do |product_attribute_type| %>
  <tr>
    <td><%= product_attribute_type.name %></td>
    <td><%= link_to 'Show', product_attribute_type %></td>
    <td><%= link_to 'Edit', edit_product_attribute_type_path(product_attribute_type) %></td>
    <td><%= link_to 'Destroy', product_attribute_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product attribute type', new_product_attribute_type_path %>
